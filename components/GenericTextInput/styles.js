import { StyleSheet } from "react-native";

export default StyleSheet.create({
   defaultInput: {
      // backgroundColor: "red",
      // flex: 0.8,
      // height: 40,
      margin: 5

      // paddingLeft: 10
      // width: "80%"
   }
});
